const Router = require("koa-router");

const router = new Router();

// Get all notes
router.get("/all", () => {});

// Get one note
router.get("/:id", () => {});

// Create new one
router.post("/", () => {});

// Update new one
router.put("/:id", () => {});

// Delete one
router.delete("/:id", () => {});


